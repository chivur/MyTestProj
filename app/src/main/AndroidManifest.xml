<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.razvan.paragontest" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/paragon_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Abilities"
            android:label="Abilities" >
        </activity>

        <activity
            android:name=".ItemCardReader"
            android:label="ItemCardReader" >
        </activity>

        <activity
            android:name=".Lore"
            android:label="Lore" >
        </activity>

    <activity
            android:name=".HeroSelected"
            android:label="@string/title_activity_hero_selected" >
        </activity>
    </application>

</manifest>
